# Run Ubuntu on Mac

รัน Ubuntu บน Mac ผ่าน VMware Fusion

## 🚀 Installation

![Install Ubuntu On Mac Using VMware Fusion Image](/images/ubuntu-vm.png)

- วีดีโอสอนติดตั้ง: <https://www.youtube.com/watch?v=jdBuSgwu1aA>
- ดาวน์โหลด Ubuntu image ที่นี่: <https://cdimage.ubuntu.com/daily-live/current/>

## 💻 Basic Commands

### อัพเดทระบบ

อัพเดทแพ็กเกจและระบบให้เป็นเวอร์ชันล่าสุด

```sh
sudo apt update && sudo apt upgrade -y
```

### ตรวจสอบ IP Address

ดู IP Address ของเครื่อง

```sh
ip a
```

### ติดตั้ง VMware Tools

ติดตั้งเครื่องมือสำหรับใช้ clipboard และ drag & drop ระหว่าง Mac กับ Ubuntu

```sh
sudo apt install open-vm-tools open-vm-tools-desktop -y
```

### ติดตั้ง curl

เครื่องมือสำหรับดาวน์โหลดไฟล์และทดสอบ API

```sh
sudo apt install -y curl
```

### ติดตั้งและตั้งค่า SSH Server

ติดตั้ง SSH Server เพื่อเข้าถึง Ubuntu จากเครื่องอื่น

**ติดตั้ง:**

```sh
sudo apt install -y openssh-server
```

**เปิดใช้งานอัตโนมัติเมื่อบูตเครื่อง:**

```sh
sudo systemctl enable ssh
```

**เริ่มต้นบริการ:**

```sh
sudo systemctl start ssh
```

**หรือรีสตาร์ท (ถ้าทำงานอยู่แล้ว):**

```sh
sudo systemctl restart ssh
```

### ตั้งค่าให้ root login ผ่าน SSH ได้

แก้ไข SSH config เพื่ออนุญาตให้ root login

**เปิดไฟล์ config:**

```sh
nano /etc/ssh/sshd_config
```

**แก้ไข:**

1. หา `PermitRootLogin prohibit-password`
2. เปลี่ยนเป็น `PermitRootLogin yes`
3. บันทึก: `Ctrl + X` → `Y` → `Enter`

**รีสตาร์ท SSH:**

```sh
sudo systemctl restart ssh
```

### ตั้งรหัสผ่าน root

**เข้าใช้งาน root:**

```sh
su -
```

**ตั้งรหัสผ่าน root:**

```sh
passwd root
```
